
@* This partial view shows a toast in the upper right corner *@

@if (TempData["success"] != null)
{
    <div class="position-fixed top-0 end-0 p-3" style="z-index: 11">
        <div id="toastr" class="toast align-items-center text-white bg-success border-0" role="alert" aria-live="assertive" aria-atomic="true">
            <div class="d-flex">
                <div class="toast-body fs-6">@TempData["success"]</div>
                <button type="button" class="btn-close btn-close-white me-2 m-auto" data-bs-dismiss="toast" aria-label="Close"></button>
            </div>
            </div>
    </div>
    <script type="text/javascript">
        window.onload = function () {
            $('#toastr').toast({ delay: 1000 });
            $('#toastr').toast('show');
        }
    </script>
}

@if (TempData["error"] != null)
{
    <div class="position-fixed top-0 end-0 p-3" style="z-index: 11">
        <div id="toastr" class="toast align-items-center text-white bg-danger border-0" role="alert" aria-live="assertive" aria-atomic="true">
            <div class="d-flex">
                <div class="toast-body fs-6 ">@TempData["error"]</div>
                <button type="button" class="btn-close btn-close-white me-2 m-auto" data-bs-dismiss="toast" aria-label="Close"></button>
            </div>
            </div>
    </div>
    <script type="text/javascript">
        window.onload = function () {
            $('#toastr').toast({ delay: 1000 });
            $('#toastr').toast('show');
        }
    </script>
}
